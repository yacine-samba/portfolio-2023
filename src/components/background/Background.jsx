import s from './Background.module.scss';

const Background = () => {
	return (
		<div className={s.background}>
			<div className={s.background__shape}></div>
		</div>
	);
};

export default Background;
